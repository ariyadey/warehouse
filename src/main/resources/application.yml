spring:
  datasource:
    url: "jdbc:h2:tcp://localhost/~/test"
    username: "warehouse"
    password: "warehouse"
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    root: info
    ir:
      asta:
        training:
          testloglevel: debug
    org:
      hibernate:
        type:
          descriptor:
            sql:
              BasicBinder: trace